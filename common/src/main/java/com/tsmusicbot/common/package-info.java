/**
 * Shared DTOs and protocol types exchanged between {@code backend} and {@code voice-agent}.
 *
 * <p>This package intentionally has no TeamSpeak-specific or framework code — see CLAUDE.md section
 * 4 for the module boundary this enforces.
 */
package com.tsmusicbot.common;
